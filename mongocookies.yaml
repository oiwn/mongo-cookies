session_name: mongocookies
windows:
  - window_name: code
    focus: true
    start-directory: ./
    panes:
      - shell_command:
          - workon mongocookies
          - empre
  - window_name: cmd
    start-directory: ./
    panes:
      - shell_command:
          - workon mongocookies
  - window_name: db
    panes:
      - shell_command:
          - mongo mongocookies
